default persistent.filefail = dict()

init -11 python in reactions:
    import store
    import random
    import datetime

    from collections import namedTuple

    GRD = namedTuple(
        "GRD",
        [
            "label",
            "g_name",
            "sprite",
        ]
    )

    frdb = dict()

    frdef = dict()

    fndredef = dict()

    gg = list()

    bg = list()

    ext = ".gift"

    def AR(ev_label, fname, _action=store.ev_rec_list, good=None):

        if fname is not None:
            fname = fname.lower()

        ev = store.Event(
            store.persistent.evdb,
            ev_label,
            category=fname,
            action=_action
        )

        frdb[ev_label] = ev
        frdef[fname] = ev

        if good is not None:
            if good:
                gg.append(ev_label)
            else:
                bg.append(ev_label)
    


        


